@model TMDTLaptop.Models.Banner

@{
    ViewBag.Title = "Thêm Banner";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container">
    <h2 class="mt-4">@ViewBag.Title</h2>

    <div class="card">
        <div class="card-body">
            @using (Html.BeginForm("ThemBanner", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                <div class="form-group">
                    <label for="HinhAnh">Hình Ảnh</label>
                    <input type="file" name="HinhAnh" class="form-control" accept="image/*" required />
                </div>

           

                <div class="form-group">
                    <label for="MoTa">Mô Tả</label>
                    @Html.TextBoxFor(m => m.MoTa, new { @class = "form-control", placeholder = "Nhập mô tả" })
                </div>

                <button type="submit" class="btn btn-success">Lưu</button>
                <a href="@Url.Action("QuanLyBanner")" class="btn btn-secondary">Quay lại</a>
            }
        </div>
    </div>
</div>
