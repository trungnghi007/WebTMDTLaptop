@model TMDTLaptop.Models.Banner

@{
    ViewBag.Title = "Sửa Banner";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container">
    <h2 class="mt-4">@ViewBag.Title</h2>

    <div class="card">
        <div class="card-body">
            @using (Html.BeginForm("SuaBanner", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.HiddenFor(m => m.MaBanner) <!-- Giữ lại Mã Banner trong form -->

                <div class="form-group">
                    <label for="HinhAnh">Hình Ảnh</label>
                    <input type="file" name="HinhAnh" class="form-control" accept="image/*" />
                    <small class="form-text text-muted">Để trống nếu không muốn thay đổi hình ảnh.</small>
                </div>

            

                <div class="form-group">
                    <label for="MoTa">Mô Tả</label>
                    @Html.TextBoxFor(m => m.MoTa, new { @class = "form-control", placeholder = "Nhập mô tả" })
                </div>

                <button type="submit" class="btn btn-success">Cập Nhật</button>
                <a href="@Url.Action("QuanLyBanner")" class="btn btn-secondary">Quay lại</a>
            }
        </div>
    </div>
</div>
